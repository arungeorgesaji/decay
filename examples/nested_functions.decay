# Demonstrates nested function calls

slow func square(x) {
    return x * x
}

slow func sum_squares(a, b) {
    return square(a) + square(b)
}

slow func main() {
    return sum_squares(3, 4)
}

main()
